#include <stdio.h>
#include <string.h>
int main(){

    // for (int i=0; i < 10; i++)
    //     printf("abcdefghijklmnop\n" + i);
    char a[32] = "Introduction", b[32] = "Programming", *s, *t;
    s = a; t = b;
    while (*s++ = *t++)
    printf("%s\n",a);
    return 0;
}